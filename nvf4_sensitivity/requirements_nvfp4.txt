# NVFP4 Sensitivity Analysis — dependencies
# Install: pip install -r requirements_nvfp4.txt
#
# Requires: Blackwell GPU (SM120/SM100), CUDA 12.8+, cuDNN 9.x

# Core
torch>=2.6.0
transformers>=4.52.3
safetensors>=0.5.0
datasets>=3.0.0
huggingface-hub>=0.30.0

# NVFP4 native compute (FlashInfer with mm_fp4)
flashinfer-python>=0.6.5

# Utilities
tqdm
numpy
